#!/usr/bin/env bash

set -efu

function main {
  set -x
  block compile virtualenv
  (set +f; cd bin && ln -nfs \
    ../vendor/virtualenv/bin/ansible \
    ../vendor/virtualenv/bin/ansible-* \
    .)
}

main "$@"
